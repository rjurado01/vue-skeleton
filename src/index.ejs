<!doctype html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Demo</title>
  </head>
  <body>
    <div id="app">
      <component :is="($route.meta.layout || 'default') + '-layout'">
        <router-view></router-view>
      </component>
    </div>
  </body>
</html>
